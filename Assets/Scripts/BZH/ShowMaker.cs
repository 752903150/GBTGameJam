using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShowMaker : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Debug.LogWarning(TOOLS.);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
